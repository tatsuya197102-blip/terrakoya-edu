# TERRAKOYA-edu 引継ぎ書(2026-07-17 作業分)

対象リポジトリ: `tatsuya197102-blip/terrakoya-edu`(ローカル: `C:\Users\User\Desktop\terrakoya-edu`)
本番URL: **https://edu.terrakoya.app**
前回引継ぎ: HANDOVER_20260712.md(モバイルペイント/マスコット/ドメイン移行/Blaze/Storage移行)

---

## 1. 今日の成果: 「うたって!マスコット」機能(/sing)を着想から完成まで本番投入

子どもが歌詞を入力すると、選んだマスコット(うさぎ/ねこ/とり)が童謡風メロディで歌う機能。
**JP/EN/AR 3言語対応(UIも歌声も切替)+ BGM + 歌の保存・共有**まで実装済み・本番動作確認済み。

### 構成ファイル
- `src/app/sing/page.tsx`(v5)
  - 歌詞入力(1〜6行、1行30文字)+キャラ選択+ステージ演出(バウンス+音符、reduced-motion対応)
  - 多言語: ページ内辞書 `STRINGS`(ja/en/ar)を `i18n.language` で切替。中央i18n辞書は**未変更**
    (Navbar paintLabel と同じインライン方式)。AR時は `dir="rtl"`
  - **ダークモード対策**: 白カード内の文字色を明示指定(`color:#111827` + iOS対策 `WebkitTextFillColor`)。
    ※サイト全体がダークモード時に文字色白になるため、白背景カードでは明示指定が必須(今後の教訓)
  - **BGM**: `public/bgm/sing_loop.mp3` を歌唱中ループ再生(volume 0.22)。ファイルが無くても声だけで動く
  - **保存**: 歌唱後に保存ボタン(要ログイン)→ Storage `songs/{uid}/{ts}.mp3` + Firestore `songs`
    コレクション {uid, name, lyrics[], character, lang, audioUrl, storagePath, createdAt}
  - **みんなの歌**: 最新20件リスト(createdAt desc)。再生/一時停止、自分の歌のみ削除
    (Firestore削除 + Storageベストエフォート削除、ギャラリーと同方式)
- `src/app/api/sing/route.ts`(v4)
  - **音符単位メロディ**: 日本語=モーラ(文字)単位(「きょ」「っ」「ー」は前の文字と結合)、
    英/亜=単語単位。**漢字混じり日本語は行単位ピッチにフォールバック**(読み崩壊防止)
  - メロディ: `PHRASES` = [ピッチst, 長さ] の童謡風4フレーズ(起承転結、きらきら星型リズム)。
    行末は必ず長音(rate 45%)、日本語は「ー」を自動付加して母音を物理的に伸ばす
    (「ん」「っ」等は `NO_STRETCH` で除外)。曲の最後の音はドに着地
  - 強弱: 高音(ソ以上)+2dB、着地音+1dB。行間ブレス450ms
  - 言語別ボイス: ja=`ja-JP-Neural2-B` / en=`en-US-Neural2-F` / ar=`ar-XA-Wavenet-A`
    (**アラビア語はNeural2が存在しないためWavenet**)
  - キャラ別基本ピッチ: rabbit +2st / cat 0 / bird +4st
  - 安全対策: NGワード(日英亜)、電話番号様数字列(6桁以上)拒否、文字数制限(行30/全体160)、
    エラーメッセージ3言語
- `public/bgm/sing_loop.mp3`: **オリジナル生成BGM**(著作権フリー)。104BPM・18.5秒シームレスループ。
  パーカッション主体(シェイカー/キック/ウッドブロック)+C中心の控えめベース
  = TTS歌声のキーとぶつからない設計。生成スクリプトは numpy でクロード側生成(必要なら再生成可能)

### 導線
- `Navbar.tsx`: `singLabel`(インライン3言語: うた/Sing/غنِّ)、デスクトップ+モバイル両ナビの🎨直後に🎤
- `dashboard/page.tsx`: クイックアクション 6→7個(`md:grid-cols-7`)
- `MascotCorner.tsx`: `HIDE_ON` に `/sing`(ステージキャラと被り防止)

### コミット履歴(全てVercel Ready確認済み)
- `102781f` feat: sing page(初版・日本語のみ)
- `0a56e78` feat: sing page i18n(ja/en/ar UI + voice switching)
- `b5fbfe8` feat: add sing to navbar/dashboard, hide corner mascot
- `581c115` fix: dark mode text visibility + note-level melody(v3)
- feat: singing expressiveness v4 + BGM + song save/share
- (最新) feat: rap mode(hip-hop flow + don-don-jan beat + club stage)

### ラップモード(v6/API v5)— 最終追加分
- **うた🎵/ラップ🎧の切替式。ラップがデフォルト**(TTSは「リズムに乗った喋り」が得意なので
  ラップの方が自然に決まる、というのが設計判断)
- API: `style` パラメータで分岐。ラップフロー = 表拍を強く高く(+3dB)・裏拍を軽く速く、
  **行末はピッチを落として遅く強く=パンチライン**。曲の最後はさらに低く着地
- BGM: `public/bgm/rap_loop.mp3`(オリジナル生成。90BPM、ドン・ドン・ジャン・休、
  808キック+スネア+8分ハット+A中心ベース)。ラップ時は音量0.3、**声の前にビートを1拍(660ms)先行再生**
- UI: ラップ時はステージが紫のクラブ風に変化、キャラは8分同期の速いバウンス、🔥💿演出。
  保存データに `style` フィールド追加、みんなの歌リストに🎧/🎵バッジ

---

## 2. Firebase/GCP設定(1回きり、済)

### Cloud TTS
- **Cloud Text-to-Speech API 有効化**(プロジェクト terrakoya-edu = 271392927832)
- **APIキー**: API制限 = Cloud Text-to-Speech API のみ / アプリケーション制限 = なし(サーバー専用)
- **Vercel環境変数**: `GOOGLE_TTS_API_KEY`。※変更後は Redeploy 必須

### 追加したセキュリティルール
- **Firestore** `songs`: read全許可 / create・delete は本人(uid一致)のみ
- **Storage** `songs/{uid}/**`: read全許可 / write は本人かつ2MB未満かつ audio/* のみ

### ハマった点(教訓)
1. **Speech-to-Text と Text-to-Speech の取り違え**: キー制限で「Cloud **Speech-to-Text** API」
   (音声→文字)を選んでしまい `API_KEY_SERVICE_BLOCKED`。必要なのは **Text-to-Speech**(文字→音声)
2. API有効化前はキー制限プルダウンにそのAPIが**表示されない**
3. キー単体テスト(トラブル切り分け用):
   ```powershell
   $key = "実際のキー"
   $json = '{"input":{"text":"こんにちは"},"voice":{"languageCode":"ja-JP","name":"ja-JP-Neural2-B"},"audioConfig":{"audioEncoding":"MP3"}}'
   $json | Out-File -Encoding utf8 tts_test.json
   curl.exe -s -X POST -H "Content-Type: application/json" -d "@tts_test.json" "https://texttospeech.googleapis.com/v1/text:synthesize?key=$key"
   ```
   エラー判別: `SERVICE_DISABLED`=API未有効化 / `API_KEY_SERVICE_BLOCKED`=キー制限ミス /
   `API_KEY_INVALID`=キー文字列違い

---

## 3. コスト

- TTS無料枠: 月100万文字(歌詞200文字換算で月5,000回無料)。超過後 $16/100万文字
- 歌mp3: 数十KB/曲 → Storage無料枠で余裕。既存の予算アラート($10)で監視
- BGM: 自作なのでライセンス費ゼロ

## 4. 歌品質の現在地と将来オプション

- 現行(SSML方式)の上限 ≒ わらべうた・お遊戯の歌レベル。**ここまでは到達済み**
- さらに上(音程保持・ビブラート・伴奏同期)は歌声合成エンジンが必要:
  - NEUTRINO: auto-checkerのVPS(2GB)同居案。無料だが生成30秒〜数分/曲(子どもは待てない)、
    メモリ検証・商用条件確認が必要 → **TERRAKOYA-pro のプロ向け機能として将来検討**が妥当
  - 商用API(Suno等): ¥10〜50/曲。無料機能には重い

## 5. 次回やること(優先順)

0. **【新規・岡本さん発案】ダッシュボードにたまごっち要素**
   - トップ(ダッシュボード)で「自分のマスコットの様子」が見える。TERRAKOYA Study と同じ体験設計
   - 狙い: ダッシュボードを「事務画面」から「自分の子に会いに来る場所」へ。家庭(Study)と
     学校(edu)でキャラ体験を接続
   - 設計イメージ(叩き台): users/{uid} に petCharacter / petXp / streak / lastActiveDate、
     状態(げんき/おなかすいた/さみしい 等)は最終活動からの経過日数、餌=レッスン完了・
     ペイント投稿・歌作成などの学習活動、XPで成長段階
   - **⚠️ 着手前に Study スレッドのたまごっち仕様(状態の種類・餌の定義・成長段階)を
     見せてもらい、両アプリで挙動を揃えること**(今回チャット検索では詳細仕様が見つからなかった)
1. **【前回からの持ち越し・最優先】他の投稿経路の Storage 化**
   (課題提出 / auto-4manga / アニメ投稿。ペイントと同方式)
2. **【前回からの持ち越し】Firestore セキュリティルールの点検**(songs追加時に全体も見ると効率的)
3. (任意)/sing 拡張: NGワードのClaude APIチェック併用、歌のギャラリー本体への統合、
   コンテスト連携(「うたコンテスト」はエジプト展開のデモ映えが良い)
4. (持ち越し)マスコット拠点別出し分け / App Check

## 6. 運用メモ(今日の追加分)

- **PowerShellの `[IO.File]::ReadAllText/WriteAllText` は必ず絶対パス**
  (プロセスのカレント=C:\Users\User 基準で解決されるため、cd していても相対パスは効かない)
- **日本語/アラビア語/絵文字をPowerShell経由で書き込む場合**は文字コードで組み立てる:
  `$uta = -join [char[]]@(0x3046,0x305F)` / `$mic = [char]::ConvertFromUtf32(0x1F3A4)`
- TypeScriptコードをPowerShellに直接貼らない(実行しようとして構文エラー。無害だが混乱のもと)
- ローカル `npm run build` の `TurbopackInternalError ... globals.css ... timeout` は Turbopack の
  不安定さ。再実行 or `.next` 削除。**Vercel側ビルドを正**とする
- ダークモード対応: 白背景を直接指定するカードには文字色も必ず明示指定(全ページ共通の教訓)
- 検証フロー: 置換後 Select-String で件数確認 → build → "N files changed" 確認 → push
