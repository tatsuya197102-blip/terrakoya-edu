# TERRAKOYA-edu 引継ぎ書（2026-07-12 作業分）

対象リポジトリ: `tatsuya197102-blip/terrakoya-edu`（ローカル: `C:\Users\User\Desktop\terrakoya-edu`）
本番URL: **https://edu.terrakoya.app** ／ 旧URL `terrakoya-edu.vercel.app` は 307 で新URLへリダイレクト
関連: Vercel（Pro プラン）／ Firebase プロジェクト `terrakoya-edu`（Blaze）／ 姉妹サイト `pro.terrakoya.app`（Firebase `terrakoya-pro`, Blaze）

---

## 1. 今期の作業サマリ（時系列）

### A. ペイント画面のモバイル対応（`src/app/paint/page.tsx`）
- 旧: PC向け横3カラム固定で、スマホではキャンバスが細い縦帯になり描けなかった
- 新: 幅 **820px 未満**で縦積みレイアウトに切替（`isNarrow` state で条件分岐、CSSはインラインstyleのまま）
  - ツール: モバイルでは上部の横1行（横スクロール可）
  - 設定パネル（ブラシ/カラー/レイヤー/ぬりえ）: 下からの**開閉ドロワー（初期は閉、最大52vh）**
  - パネル内の並び順: ブラシ → カラー → レイヤー → **ぬりえ（最下部）**
- キャンバス比率を **A4縦（W=900, H=1273）** に変更（旧 900x1200）
- フィット: `ResizeObserver` でキャンバス領域を監視し自動ズーム。モバイルは**横幅優先**（縦はスクロール）、PCは全体フィット
- キャンバスは「表示サイズの枠 + 左上基準scale」構造（flexに潰されない）
- 主要コミット: `2bb857f`(枠構造) → `572ace7`(A4+横幅優先) → `87e08dc`(ぬりえ最下部)

### B. マスコット導入（うさぎ/ねこ/とり の3キャラ、デザインは支給PNGそのまま）
- 画像: `public/mascots/{rabbit|cat|bird}_{96|128|256|512}.png`（透過・正方形化済み）
- `src/components/Mascot.tsx` … `<Mascot/>` 共通コンポーネント
  - `name` でキャラ固定 / `seed` で場所ごと固定（同seed=同キャラ） / 指定なしでランダム / `rounded` で丸アバター
- `src/components/MascotCorner.tsx` … 全ページ右下/左下にふわふわ浮く常駐マスコット
  - `client-wrapper.tsx` に `<MascotCorner/>` を1行追加して全ページ適用
  - `/paint` と `/auto-animate` では自動非表示（HIDE_ON 配列で制御）／クリック透過／スマホは縮小表示
- 設置箇所:
  - ファビコン `src/app/icon.png`（ねこ）+ `apple-icon.png`
  - Navbar: ロゴ横（seed=pathname でページごとに切替）＋ 写真未設定ユーザーの初期アバター
  - ログイン: アプリ名上に96px（seed="login"）
  - ダッシュボード: ウェルカム横76px（seed=ユーザー名）/ ローディング72px（bounce）/ コース未受講の空状態88px
  - まちがいさがし（`src/app/game-spot/page.jsx`）: `SPRITES` に rabbit/cat/bird を追加し探す対象に。
    画像には色相/形ペア差分が効かないため `MASCOT` Set でガードし size/move に振替済み
- 主要コミット: `6add8b7`(assets+favicon) → `a8ce509`(navbar) → `19736d3`(login/dashboard) → `8411016`(corner) → `44f99c0`(game)

### C. カスタムドメイン移行
- Vercel の `terrakoya.app`（Vercel管理DNS）の Connected Projects で `edu` → `terrakoya-edu` を接続
- `terrakoya-edu.vercel.app` → `edu.terrakoya.app` へ **307 リダイレクト**設定済み（恒久化するなら 308 に変更可）
- **ログイン不可障害と対処**: 新ドメインで Google ログイン失敗
  - Firebase Auth の承認済みドメインには追加済みだった
  - 真因は **Google Cloud Console → APIとサービス → 認証情報 → OAuth 2.0 Web client** 側。
    「承認済みJavaScript生成元」に `https://edu.terrakoya.app`、
    「承認済みリダイレクトURI」に `https://edu.terrakoya.app/__/auth/handler` を追加して解消
  - ※今後別サブドメインを足すときも同じ2点セット（Firebase承認済みドメイン + OAuth設定）が必要

### D. コスト/スケール対策（〜5万人想定）
- **Firebase Blaze 化**: `terrakoya-edu` を Spark→Blaze（`terrakoya-pro` は既存Blaze）。同一請求先アカウントに集約
- **予算アラート**: edu=$10、pro=¥1,000（Google Cloud「予算とアラート」、50/90/100%通知）。現状利用額 ¥0
- **投稿画像の Storage 移行**（最重要変更）:
  - 旧: ペイント投稿はJPEGを base64(≦約900KB) で Firestore `submissions` に直接保存 → 読み取りコスト爆発の主犯
  - 新: `paint/page.tsx` の `doPublish` が **Firebase Storage `submissions/{uid}/{timestamp}.jpg`** にアップし、
    Firestore には `imageUrl`(ダウンロードURL) と `storagePath` のみ保存。`toDataUrl()` は `toJpegBlob()` に置換
  - `gallery/page.tsx`: 削除時に `storagePath`（無ければ firebasestorage URL）で Storage 画像もベストエフォート削除。
    旧base64投稿は互換維持（表示・削除ともそのまま動く）。`<img loading="lazy">` 追加
  - Storage バケット: `terrakoya-edu.firebasestorage.app`、ロケーション **US-EAST1（料金不要枠）**
  - Storage ルール（公開済み）: `submissions/{uid}/**` は read 全許可 / write は本人かつ3MB未満かつ image/* のみ、他パスは全拒否
  - 動作確認済み: 投稿1件で Storage に 20.7KB の jpg 生成を確認
  - コミット: `e2b8822`
  - ※途中で空振りコミット（`9c024f4`, .gitignore改行のみ）を作って `git reset --soft HEAD~1` で取り消した経緯あり。履歴上は問題なし

---

## 2. 次回やること（優先順）

1. **他の投稿経路の Storage 化**
   - 対象候補: コース課題提出（`src/app/courses/[id]/assignment/page.tsx`）、4コマ（`auto-4manga`）、アニメ投稿
   - 課題提出はカメラ写真でペイントより重くなりがち。ペイントと同じ方式
     （`toJpegBlob`→`uploadBytes`→`getDownloadURL`→Firestoreは `imageUrl`+`storagePath`）に揃える
   - Storage ルールは既存の `submissions/{uid}/` を流用できる（パスを合わせる）
2. **Firestore セキュリティルールの点検**
   - `submissions` への write 権限・他人ドキュメントの改ざん可否（いいね/リアクションは client から直接 `updateDoc` する設計）
   - Firestore → ルール タブの現物を見て診断するところから
3. （余裕があれば）**マスコットの拠点別出し分け**
   - 案: うさぎ(甲冑)=日本/制作系（レッスン・コース・ペイント・4コマ）、ねこ(バステト)=エジプト/コンテスト・ギャラリー、
     とり(ホルス)=見せる系（ショーケース・証明書・ライブ）
   - `Mascot.tsx` に「ページ→キャラ」対応表を持たせれば Navbar/Corner とも一括で揃う
4. （中期）App Check 有効化（数千人規模になったら）／ 旧base64投稿の一括変換スクリプト（必要になったら）

---

## 3. 運用メモ・ハマりどころ

- **作業フロー**: Claude がファイル生成 → Downloads へ保存 → PowerShell でプロジェクトにコピー → `npm run build` → `git add/commit/push` → Vercel 自動デプロイ（1〜2分）
  - コピー前に必ず `Write-Host` でコピー元ファイル名を確認（**Downloadsに目的ファイルが無いまま進めて空コミットになった事故あり**）
  - コミット出力の "N files changed" が想定通りか確認してから push
- **キャッシュ**: デプロイ後に変化が見えないときは、まずシークレットウィンドウで確認 → 原因がキャッシュなら
  F12 → リロードボタン右クリック → 「キャッシュの消去とハード再読み込み」
- **ビルド時間**: `npm run build` は約3.5〜4.5分（Turbopack）
- ペイントのファイルは `src/app/paint/page.tsx`（**src/ 配下**。`app/paint/` 直下ではない）
- game-spot は **page.jsx**（tsxではない）
- Navbar のヘッダーは `src/components/Navbar.tsx`、全ページ共通の入口は `src/app/client-wrapper.tsx`
- 承認済みドメイン一覧（Firebase Auth）: localhost / terrakoya-edu.firebaseapp.com / terrakoya-edu.web.app /
  terrakoya-edu.vercel.app / employment-lms.vercel.app / edu.terrakoya.app

## 4. 現在の月額コスト
- Vercel Pro: $20
- Firebase edu/pro (Blaze): 現状 ¥0（無料枠内）、予算アラート設定済み
- ドメイン terrakoya.app: $15/年
