# TERRAKOYA-edu 引継ぎ書(2026-07-25 作業分)— 学校ペット機能 Phase 1〜2 完成

対象リポジトリ: `tatsuya197102-blip/terrakoya-edu`(ローカル: `C:\Users\User\Desktop\terrakoya-edu`)
本番URL: **https://edu.terrakoya.app**
前回引継ぎ: HANDOVER_20260717.md(うたって!マスコット/ラップモード/たまごっち構想)

---

## 1. 今日の成果: 「がっこうの子」(学校ペット)を設計転換から本番完動まで

前回引継ぎ§5-0の「たまごっち要素」を、**着想と違う形で**完成させた。本番E2E確認済み
(活動クリア→ねこ起床→♥1/20 をスクショで確認)。

### 1-1. 設計の転換(重要。次回以降もこの前提で)

| 論点 | 当初案(v1) | 最終形(v2)= 本番 |
|---|---|---|
| 単位 | クラスに1体(先生がクラス編成) | **全校で1体 `pets/school`(セットアップ0)** |
| 転換理由 | 先生ロールが**未使用**のため、クラス前提だとペットが誰にも届かない | 児童は1人1台端末なので個人統計は生存 |
| edu/Study差別化 | — | **eduは所有させない**(=学校のみんなの子)。所有・進化・⭐はStudyの売り物 |
| 状態設計 | — | 死・飢え・衰弱なし。♥0の日は「ねんね中」、7日ぶりは「まってたよ!」(Study審査安全設計と共通) |
| 通貨 | — | edu=♥(その日限り) / Study=⭐(蓄積)。**名前を分けて衝突回避** |
| 課金訴求 | — | edu本体には一切出さない(B2G前提)。導線は保護者ページ/学期末アルバムPDF/おたよりの3箇所限定 |

仕様書: `TERRAKOYA-edu_学校ペット仕様_v2.md`(v1のクラス依存版は破棄)

### 1-2. 実装(コミット順)

- `6616144` feat: school pet card (Phase 1) — `src/lib/pet.ts` + `src/components/SchoolPet.tsx` + dashboard組込
- `d81f550` chore: .gitignoreに *.bak_* / tts_test.json 追加+リポジトリから除去
- `a442341` feat: マスコット画像対応(絵文字フォールバック付き)
- `ad739ef` assets: `public/pets/{usagi,neko,tori}.png`(ファラオ風3体、透過・各50〜82KB)
- `c739637` feat: 4活動に給餌差し込み(sing/paint/auto-4manga×2経路/レッスン初回完了)
- `0eb6df9` feat: まちがいさがしクリアに給餌(5/5活動完了)

### 1-3. データ構造(本番稼働中)

```
pets/school
  character('cat'), dailyGoal(20), todayDate, todayHearts, termHearts, termStartDate, updatedAt
pets/school/feeds/{YYYY-MM-DD}
  total, byUser: { uid: 0..3 }        // 1人1日3上限
users/{uid}
  lastActiveDay('YYYY-MM-DD'),        // ★Studyとキー名統一(将来のアカウント統合用)
  termHearts
```

- 初期ドキュメント作成不要: 初回給餌時に feedPet が既定値(cat/goal20)で自動作成
- 日次リセットは cron 不要(当日初給餌時のトランザクション内で切替)
- キャラ変更・目標変更はコンソールで `pets/school` の `character` / `dailyGoal` を直接編集
- **Firestoreルールは公開済み**: `pets/**` read=要ログイン / create,update=要ログイン / delete=不可。
  厳格化(差分+1検証・admin限定)は持ち越しのルール全体点検と同時に

### 1-4. 給餌ポイント(feedPetの差し込み箇所)

| 活動 | ファイル | 発火条件 |
|---|---|---|
| 歌・ラップ保存 | sing/page.tsx | 保存成功後 |
| ペイント投稿 | paint/page.tsx | 投稿成功後 |
| 4コマ | auto-4manga/page.tsx | 提出成功後(**ファイル/ペイントの2経路とも**) |
| レッスン | courses/[id]/lessons/[lessonId]/page.tsx | **初回完了のみ**(isNewCompletion) |
| まちがいさがし | **game-spot/page.jsx**(唯一の.jsx!) | クリア(won)時。連打対策は3上限で担保 |

全箇所 `.catch(() => {})` で本来処理と分離(給餌失敗が保存を巻き込まない)。

---

## 2. Study側との整合(同日にStudy側も本番化)

Study側は同日、育成システム Phase A/B(1体→進化5段階・⭐経済・仲間迎え)+E2Eフルループが本番化。
つまり **「学校のねこ」と「うちの子」が同日に両輪稼働**。edu→Studyの橋(Phase 3)の土台は両側完成。

- 統一済み: `users/{uid}.lastActiveDay` のキー名、審査安全設計(死なせない・後退しない・再会報酬)
- キャラ画像は同じファラオ風3体(edu側は1500x500.jpg市松背景をflood fillで除去して生成)
- Study側の課金壁=最終進化。eduで成体を見せるのは「自分も育てたい」の動機になり競合しない

## 3. 次回やること(優先順)

1. **Phase 3: 学期末アルバムPDF+たまご引換コード**
   - その学期の歌/ペイント/4コマがマスコットと並ぶPDF。最終ページにコード+QR
   - コードは学校単位1つ・使用上限=想定人数。**edu側は発行と保存まで、検証はStudy側実装**
   - Study側と並走が必要 → Studyスレッドに「edu引換コードの受け入れ」を依頼する段取りから
2. **(持ち越し)他の投稿経路のStorage化**(課題提出/auto-4manga/アニメ投稿)
3. **(持ち越し)Firestoreルール全体点検** — pets/** の厳格化(差分+1検証)もここで
4. (任意)給餌時のトースト演出(「ねこちゃんが よろこんだ!」)— 現状は無通知で♥だけ増える
5. (将来・Phase 4)クラス機能連動+先生用児童別一覧 — **先生ロールの実利用開始が前提条件**

## 4. 運用メモ(今日の教訓)

- **PowerShellに複数行の if/elseif/else を直接貼ると行単位で分断されて壊れる**
  → 以降、複数行コマンドはすべて `& { ... }` で1ブロックに包んで渡す運用(今日から統一済み)
- **page.tsx検索では game-spot が漏れる**: 唯一の `.jsx`。全ページ検索は `-Include page.tsx,page.jsx,page.js`
- **Pythonでファイルをパッチする時は `newline=''`**(universal newlinesがCRLFをLFに変換してアンカー不一致になる)。
  リポジトリはCRLF/LF混在(auto-4mangaは1ファイル内で混在)なので改行保持が必須
- 市松背景のJPGは透過ではない(焼き込み)。外周flood fillで背景除去→キャラ本体の同系色は無傷
- Firebaseコンソールのルール画面は URL直行が早い:
  `https://console.firebase.google.com/project/terrakoya-edu/firestore/rules`
- ルール全文の受け渡しは「ファイル→ Set-Clipboard → コンソールで Ctrl+A/Ctrl+V/公開」方式が確実
  (日本語コメントの文字化け回避)

## 5. 新スレッドの始め方

1. このファイル+`TERRAKOYA-edu_学校ペット仕様_v2.md` を添付
2. 「**Phase 3(アルバムPDF+たまご引換コード)から**」と指示
3. Study側スレッドには「eduのたまご引換コード受け入れ(コード検証→該当キャラのたまごで開始)」を
   このファイル添付で依頼
